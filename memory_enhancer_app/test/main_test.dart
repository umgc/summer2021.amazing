// This is a basic Flutter widget test.
//
// To perform an interaction with a widget in your test, use the WidgetTester
// utility that Flutter provides. For example, you can send tap and scroll
// gestures. You can also use WidgetTester to find child widgets in the widget
// tree, read text, and verify that the values of widget properties are correct.

//import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:memory_enhancer_app/main.dart';

void main() {
  testWidgets('Check texts on the main page', (WidgetTester tester) async {

   // Build our app and trigger a frame.
   //await tester.pumpWidget(MemoryEnhancerApp());

   // Verify that our counter starts at 0.
    /*
   expect(find.text('Memory Enhancer'), findsNothing);
   expect(find.text('Home'), findsNothing);
   expect(find.text('Notes'), findsNothing);
   expect(find.text('Settings'), findsNothing);
   expect(find.text('Help'), findsNothing);
   
     */

   // Tap the 'mic' icon and trigger a frame.
   //await tester.tap(find.byIcon(Icons.mic_none));
   //await tester.pump();
  });
}
